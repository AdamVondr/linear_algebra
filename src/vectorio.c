#include <stdio.h>
#include <vector.h>

#ifdef vector

void vector_print(vector input_vector)
{
    char char_buffer; 
    for (int i = 0; i < input_vector.size; i++)
    {
        //input_vector.arr[i]
    }
}

#endif
